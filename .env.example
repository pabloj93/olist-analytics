# --- Databricks workspace credentials ---
# Get these from your Databricks Free Edition workspace:
#   HOST: settings -> workspace URL (e.g. https://dbc-xxxx.cloud.databricks.com)
#   TOKEN: user settings -> developer -> access tokens -> generate
#   HTTP_PATH: SQL warehouse -> connection details -> HTTP path
DATABRICKS_HOST=https://<your-workspace>.cloud.databricks.com
DATABRICKS_TOKEN=dapi-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/<warehouse-id>

# --- Unity Catalog naming ---
# Run `SHOW CATALOGS` in your workspace to list available catalogs.
# On most Databricks Free Edition workspaces, `workspace` is the default catalog.
# If `workspace` does not exist, pick any catalog you have CREATE SCHEMA permission on.
DATABRICKS_CATALOG=workspace

# --- dbt schemas (one catalog, three schemas: raw / dev / prod) ---
DBT_SCHEMA_RAW=olist_raw
DBT_SCHEMA_DEV=olist_dev
DBT_SCHEMA_PROD=olist_prod

# `dev` = your local dbt runs; `prod` = chatbot + Power BI read from here
DBT_TARGET=dev
