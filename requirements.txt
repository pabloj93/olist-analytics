# dbt + Databricks adapter
dbt-core==1.9.0
dbt-databricks==1.9.0

# Raw ingestion: upload Olist CSVs to Unity Catalog
# databricks-sdk pinned to 0.17.0 to match dbt-databricks 1.9.0 requirement
databricks-sdk==0.17.0
pandas==2.2.3

# SQL client used by load_raw.py for DDL + CTAS against the SQL warehouse
databricks-sql-connector==3.5.0

# Kaggle dataset download (requires ~/.kaggle/kaggle.json with API token)
kaggle==1.6.17

# .env loader (read by both dbt profiles.yml and scripts/load_raw.py)
# The [cli] extra installs the `dotenv` command used to wrap dbt invocations.
python-dotenv[cli]==1.0.1
